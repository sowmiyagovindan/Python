node{
     stage('Checkout'){
        checkout scm
     }
     stage('Example Build') {
        workspace = pwd()
        echo "${workspace}"
        env.PATH = "/usr/bin:${env.PATH}"
        echo "${env.PATH}"
        sh 'python3 HW.py'



        }

}
